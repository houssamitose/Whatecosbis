export const runtime="nodejs";export const dynamic="force-dynamic";
function sbHeaders(){return{apikey:process.env.SUPABASE_SERVICE_ROLE_KEY||"",Authorization:`Bearer ${process.env.SUPABASE_SERVICE_ROLE_KEY||""}`,"Content-Type":"application/json"};}
export async function GET(){try{const r=await fetch(`${process.env.SUPABASE_URL}/rest/v1/ss_tracked_ids?select=order_id&order=order_id.desc&limit=5000`,{headers:sbHeaders(),cache:"no-store"});if(!r.ok)return Response.json({success:false,error:`Supabase HTTP ${r.status}`},{status:r.status});const rows=await r.json();return Response.json({success:true,count:rows.length,ids:rows.map(x=>x.order_id)});}catch(err){return Response.json({success:false,error:err.message},{status:500});}}
export async function POST(req){try{const body=await req.json().catch(()=>({}));const add=Array.isArray(body.add)?body.add.map(n=>parseInt(n,10)).filter(n=>Number.isFinite(n)):[];const remove=Array.isArray(body.remove)?body.remove.map(n=>parseInt(n,10)).filter(n=>Number.isFinite(n)):[];if(add.length){const r=await fetch(`${process.env.SUPABASE_URL}/rest/v1/ss_tracked_ids?on_conflict=order_id`,{method:"POST",headers:{...sbHeaders(),Prefer:"resolution=ignore-duplicates,return=minimal"},body:JSON.stringify(add.map(order_id=>({order_id})))});if(!r.ok)return Response.json({success:false,error:`Add HTTP ${r.status}`},{status:r.status});}if(remove.length){const qs=`order_id=in.(${remove.join(",")})`;const r=await fetch(`${process.env.SUPABASE_URL}/rest/v1/ss_tracked_ids?${qs}`,{method:"DELETE",headers:{...sbHeaders(),Prefer:"return=minimal"}});if(!r.ok)return Response.json({success:false,error:`Remove HTTP ${r.status}`},{status:r.status});}return Response.json({success:true,added:add.length,removed:remove.length});}catch(err){return Response.json({success:false,error:err.message},{status:500});}}
